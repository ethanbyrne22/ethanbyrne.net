---
layout: base.njk
---
{# All projects sorted by order #}
{% set allProjects = collections.projects %}
{% set letters = collections.letters %}

<div class="front-page" id="frontPage">
  <header class="masthead">
    <svg class="masthead-svg" onclick="window.scrollTo({top:0,behavior:'smooth'})" viewBox="0 0 452.04 41.89" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Ethan Jack Smithe Byrne">{% include "masthead-inner.svg" %}</svg>
    <div class="masthead-subtitle">{{ site.subtitle }}</div>
  </header>

  <div class="dateline-bar">
    <span><a href="/about/">About</a> · <a href="mailto:{{ site.email }}">Contact</a> · <a href="https://instagram.com/{{ site.instagram }}" target="_blank">Instagram</a></span>
    <span id="currentDate"></span>
    <span class="dateline-stars">★★★★ $2.00</span>
  </div>

  <div class="ticker-bar" id="tickerBar">
    <div class="ticker-item"><span class="ticker-label">DJIA</span> <span class="ticker-val" id="t-DIA">—</span> <span class="ticker-chg" id="tc-DIA"></span></div>
    <div class="ticker-item"><span class="ticker-label">NASDAQ</span> <span class="ticker-val" id="t-QQQ">—</span> <span class="ticker-chg" id="tc-QQQ"></span></div>
    <div class="ticker-item"><span class="ticker-label">S&amp;P 500</span> <span class="ticker-val" id="t-SPY">—</span> <span class="ticker-chg" id="tc-SPY"></span></div>
    <div class="ticker-item"><span class="ticker-label">10-YR TREAS</span> <span class="ticker-val" id="t-TLT">—</span> <span class="ticker-chg" id="tc-TLT"></span></div>
    <div class="ticker-item"><span class="ticker-label">OIL</span> <span class="ticker-val" id="t-USO">—</span> <span class="ticker-chg" id="tc-USO"></span></div>
    <div class="ticker-item"><span class="ticker-label">GOLD</span> <span class="ticker-val" id="t-GLD">—</span> <span class="ticker-chg" id="tc-GLD"></span></div>
    <div class="ticker-item"><span class="ticker-label">EURO</span> <span class="ticker-val" id="t-FXE">—</span> <span class="ticker-chg" id="tc-FXE"></span></div>
    <div class="ticker-item"><span class="ticker-label">YEN</span> <span class="ticker-val" id="t-FXY">—</span> <span class="ticker-chg" id="tc-FXY"></span></div>
  </div>

  {# ============ MAIN 3-COLUMN GRID (projects 1-6) ============ #}
  <div class="main-grid">
    {# LEFT COLUMN: project 3 + Letters from the Editors #}
    <div class="col-left">
      {% set leftMain = allProjects[2] %}
      {% if leftMain %}
      <div class="story" onclick="openDetail(2)">
        <div class="story-kicker">{{ leftMain.data.kicker }}</div>
        <img class="story-image {% if leftMain.data.thumbnail_aspect == 'portrait' %}portrait{% elif leftMain.data.thumbnail_aspect == 'square' %}square{% endif %}" src="{{ leftMain.data.thumbnail }}" alt="{{ leftMain.data.title }}">
        <div class="story-headline md">{{ leftMain.data.title }}</div>
        <div class="story-byline">{{ leftMain.data.brand }}</div>
        <div class="story-excerpt">{{ leftMain.data.description | truncate(140) }}</div>
      </div>
      {% endif %}

      <div class="whats-news">
        <div class="whats-news-title">Letters from the Editors</div>
        {% if letters | length > 0 %}
          {% for letter in letters %}
          <a class="whats-news-item" href="/letters/{{ letter.fileSlug }}/">
            <div class="wn-headline">{{ letter.data.title }}</div>
            <div class="wn-client">{{ letter.data.date | dateDisplay }}</div>
          </a>
          {% endfor %}
        {% else %}
          <div style="font-family:var(--font-body);font-size:.78rem;color:var(--text);padding:5px 0;font-style:italic;">Coming soon...</div>
        {% endif %}
      </div>
    </div>

    <div class="col-rule"></div>

    {# CENTER COLUMN: project 1 (lead) + project 2 (secondary with thumbnail) #}
    <div class="col-center">
      {% set lead = allProjects[0] %}
      {% if lead %}
      <div class="story" onclick="openDetail(0)">
        <div class="story-kicker">{{ lead.data.kicker }}</div>
        <div class="story-headline xl">{{ lead.data.title }}</div>
        <div class="story-byline">{{ lead.data.brand }} · {{ lead.data.role }}</div>
        <div class="hero-media-wrapper">
          {% if site.hero_video_url %}
            <iframe class="hero-video-iframe" src="{{ site.hero_video_url | videoEmbed }}?autoplay=1&mute=1&loop=1&controls=0&playsinline=1&rel=0&showinfo=0&modestbranding=1" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen loading="lazy"></iframe>
          {% else %}
            <img src="{{ lead.data.thumbnail }}" alt="{{ lead.data.title }}">
          {% endif %}
        </div>
        {% if lead.data.thumbnail_caption %}<div class="story-caption">{{ lead.data.thumbnail_caption }}</div>{% endif %}
        <div class="story-excerpt">{{ lead.data.description }}</div>
      </div>
      {% endif %}

      {% set secondary = allProjects[1] %}
      {% if secondary %}
      <div class="story" onclick="openDetail(1)" style="border-top: 1px solid var(--rule-light); padding-top: 8px;">
        <div class="story-headline lg">{{ secondary.data.title }}</div>
        <div class="story-byline">{{ secondary.data.brand }} · {{ secondary.data.role }}</div>
        <img class="story-image {% if secondary.data.thumbnail_aspect == 'portrait' %}portrait{% elif secondary.data.thumbnail_aspect == 'square' %}square{% endif %}" src="{{ secondary.data.thumbnail }}" alt="{{ secondary.data.title }}">
        <div class="story-excerpt">{{ secondary.data.description | truncate(180) }}</div>
      </div>
      {% endif %}
    </div>

    <div class="col-rule"></div>

    {# RIGHT COLUMN: projects 4, 5, 6 + Selected Clients #}
    <div class="col-right">
      {% set rightMain = allProjects[3] %}
      {% if rightMain %}
      <div class="story" onclick="openDetail(3)">
        <div class="story-kicker">{{ rightMain.data.kicker }}</div>
        <div class="story-headline lg">{{ rightMain.data.title }}</div>
        <div class="story-byline">{{ rightMain.data.brand }}</div>
        <img class="story-image {% if rightMain.data.thumbnail_aspect == 'portrait' %}portrait{% elif rightMain.data.thumbnail_aspect == 'square' %}square{% endif %}" src="{{ rightMain.data.thumbnail }}" alt="{{ rightMain.data.title }}">
        <div class="story-excerpt">{{ rightMain.data.description | truncate(140) }}</div>
      </div>
      {% endif %}

      {% set rightSecondary = allProjects[4] %}
      {% if rightSecondary %}
      <div style="border-top: 1px solid var(--rule-light); padding-top: 8px;">
        <div class="story" onclick="openDetail(4)">
          <div class="story-headline md">{{ rightSecondary.data.title }}</div>
          <div class="story-byline">{{ rightSecondary.data.brand }} · {{ rightSecondary.data.role }}</div>
          <img class="story-image {% if rightSecondary.data.thumbnail_aspect == 'portrait' %}portrait{% elif rightSecondary.data.thumbnail_aspect == 'square' %}square{% endif %}" src="{{ rightSecondary.data.thumbnail }}" alt="{{ rightSecondary.data.title }}">
          {% if rightSecondary.data.thumbnail_caption %}<div class="story-caption">{{ rightSecondary.data.thumbnail_caption }}</div>{% endif %}
        </div>
      </div>
      {% endif %}

      {% set rightThird = allProjects[5] %}
      {% if rightThird %}
      <div style="border-top: 1px solid var(--rule-light); padding-top: 8px;">
        <div class="story" onclick="openDetail(5)">
          <div class="story-headline md">{{ rightThird.data.title }}</div>
          <div class="story-byline">{{ rightThird.data.brand }}</div>
          <img class="story-image {% if rightThird.data.thumbnail_aspect == 'portrait' %}portrait{% elif rightThird.data.thumbnail_aspect == 'square' %}square{% endif %}" src="{{ rightThird.data.thumbnail }}" alt="{{ rightThird.data.title }}">
          <div class="story-excerpt">{{ rightThird.data.description | truncate(100) }}</div>
        </div>
      </div>
      {% endif %}

      <div style="border-top: 3px solid var(--text); padding-top: 6px; margin-top: 4px;">
        <div class="whats-news-title">Selected Clients</div>
        <div style="font-family: var(--font-body); font-size: 0.75rem; color: var(--text); line-height: 1.45;">{{ site.clients }}</div>
      </div>
    </div>
  </div>

  {# ============ ADDITIONAL STORIES (projects 7+) as rows of 3 ============ #}
  {% if allProjects | length > 6 %}
  <div class="additional-stories">
    <div class="additional-stories-grid">
      {% for p in allProjects %}
        {% if loop.index0 >= 6 %}
          <div class="additional-story" onclick="openDetail({{ loop.index0 }})">
            <img class="story-image" src="{{ p.data.thumbnail }}" alt="{{ p.data.title }}">
            <div class="story-headline md" style="margin-top:5px;">{{ p.data.title }}</div>
            <div class="story-byline">{{ p.data.brand }}</div>
            <div class="story-excerpt">{{ p.data.description | truncate(120) }}</div>
          </div>
        {% endif %}
      {% endfor %}
    </div>
  </div>
  {% endif %}

  <footer class="colophon">
    <span>&copy; 2026 Ethan Byrne · Boston Polar Media</span>
    <div class="colophon-links">
      <a href="mailto:{{ site.email }}">{{ site.email }}</a>
      <a href="https://instagram.com/{{ site.instagram }}" target="_blank">@{{ site.instagram }}</a>
    </div>
  </footer>
</div>

{# DETAIL OVERLAY #}
<div class="detail-overlay" id="detailOverlay">
  <div class="detail-header-bar">
    <div class="detail-header-inner">
      <div class="detail-back" onclick="closeDetail()">&larr; Back to Front Page</div>
      <svg class="detail-masthead-svg" onclick="closeDetail()" viewBox="0 0 452.04 41.89" xmlns="http://www.w3.org/2000/svg" aria-label="EJSB">{% include "masthead-inner.svg" %}</svg>
      <div style="width: 120px;"></div>
    </div>
  </div>
  <div class="detail-content" id="detailContent"></div>
</div>

{# Embed project data as JSON for detail overlay #}
<script>
var PROJECTS = [
{% for p in allProjects %}
  { name: {{ p.data.title | dump | safe }}, brand: {{ p.data.brand | dump | safe }}, role: {{ p.data.role | dump | safe }}, kicker: {{ p.data.kicker | dump | safe }}, description: {{ p.data.description | dump | safe }}, videoUrl: {{ (p.data.video_url or "") | dump | safe }}, videos: [{% if p.data.videos %}{% for v in p.data.videos %}{url:{{ v.url | dump | safe }},caption:{{ (v.caption or "") | dump | safe }}}{% if not loop.last %},{% endif %}{% endfor %}{% endif %}], images: [{% for img in p.data.images %}"{{ img.url }}"{% if not loop.last %},{% endif %}{% endfor %}], imageAspects: [{% for img in p.data.images %}"{{ img.aspect | default('landscape') }}"{% if not loop.last %},{% endif %}{% endfor %}] }{% if not loop.last %},{% endif %}
{% endfor %}
];

document.getElementById('currentDate').textContent = new Date().toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' });

function openDetail(idx) {
  var p = PROJECTS[idx];
  if (!p) return;
  var overlay = document.getElementById('detailOverlay');
  var content = document.getElementById('detailContent');

  // Build video embeds
  var videoHTML = '';
  var allVideos = [];
  if (p.videos && p.videos.length > 0) {
    p.videos.forEach(function(v) { allVideos.push({url: v.url, caption: v.caption}); });
  }
  if (allVideos.length === 0 && p.videoUrl) {
    allVideos.push({url: p.videoUrl, caption: ''});
  }
  allVideos.forEach(function(v) {
    var embedSrc = parseVideoUrl(v.url);
    if (embedSrc) {
      videoHTML += '<div class="detail-video-wrapper"><iframe src="' + embedSrc + '?autoplay=1&mute=1&loop=1&rel=0&modestbranding=1&playsinline=1" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe></div>';
      if (v.caption) {
        videoHTML += '<div class="detail-video-caption">' + v.caption + '</div>';
      }
    }
  });

  // Build gallery with smart portrait handling
  var galleryHTML = '';
  var i = 0;
  while (i < p.images.length) {
    var src = p.images[i];
    var aspect = (p.imageAspects && p.imageAspects[i]) || 'landscape';
    var nextAspect = (i + 1 < p.images.length && p.imageAspects) ? p.imageAspects[i + 1] : 'landscape';

    if (aspect === 'portrait' && nextAspect === 'portrait' && i + 1 < p.images.length) {
      galleryHTML += '<div class="detail-gallery-pair"><img src="' + src + '" alt="' + p.name + '"><img src="' + p.images[i+1] + '" alt="' + p.name + '"></div>';
      i += 2;
    } else if (aspect === 'portrait') {
      galleryHTML += '<div class="detail-gallery-portrait"><img src="' + src + '" alt="' + p.name + '"></div>';
      i++;
    } else {
      galleryHTML += '<div><img src="' + src + '" alt="' + p.name + '"></div>';
      i++;
    }
  }

  content.innerHTML = '<div class="detail-kicker">' + p.kicker + '</div><div class="detail-title">' + p.name + '</div><div class="detail-byline-block"><div class="detail-client">' + p.brand + ' · ' + p.role + '</div><div class="detail-date">2024–2025</div></div><div class="detail-description">' + p.description + '</div>' + videoHTML + '<div class="detail-gallery">' + galleryHTML + '</div>';
  overlay.classList.add('active');
  overlay.scrollTop = 0;
  document.body.style.overflow = 'hidden';
}

function parseVideoUrl(url) {
  if (!url) return null;
  var ytMatch = url.match(/(?:youtube\.com\/(?:watch\?v=|embed\/)|youtu\.be\/)([a-zA-Z0-9_-]{11})/);
  if (ytMatch) return 'https://www.youtube.com/embed/' + ytMatch[1];
  var vmMatch = url.match(/vimeo\.com\/(\d+)/);
  if (vmMatch) return 'https://player.vimeo.com/video/' + vmMatch[1];
  return null;
}
function closeDetail() { document.getElementById('detailOverlay').classList.remove('active'); document.body.style.overflow = ''; }
document.addEventListener('keydown', function(e) { if (e.key === 'Escape') closeDetail(); });

// Dynamic stock ticker via Finnhub free API
(function() {
  var FINNHUB_KEY = 'd75hic1r01qk56kcqbl0d75hic1r01qk56kcqblg';
  var symbols = ['DIA','QQQ','SPY','TLT','USO','GLD','FXE','FXY'];
  var prefixes = {DIA:'$',QQQ:'',SPY:'',TLT:'',USO:'$',GLD:'$',FXE:'$',FXY:''};
  if (!FINNHUB_KEY) return;
  symbols.forEach(function(sym) {
    fetch('https://finnhub.io/api/v1/quote?symbol=' + sym + '&token=' + FINNHUB_KEY)
      .then(function(r) { return r.json(); })
      .then(function(d) {
        if (!d || !d.c) return;
        var valEl = document.getElementById('t-' + sym);
        var chgEl = document.getElementById('tc-' + sym);
        if (!valEl) return;
        var price = d.c;
        var change = d.d;
        var pct = d.dp;
        valEl.textContent = (prefixes[sym] || '') + price.toLocaleString(undefined, {minimumFractionDigits:2, maximumFractionDigits:2});
        if (change !== null && change !== undefined) {
          var isUp = change >= 0;
          var tri = isUp ? '▲' : '▼';
          chgEl.className = 'ticker-chg ' + (isUp ? 'ticker-up' : 'ticker-down');
          chgEl.innerHTML = '<span class="ticker-tri">' + tri + '</span>' + Math.abs(pct).toFixed(1) + '%';
        }
      })
      .catch(function() {});
  });
})();
</script>
